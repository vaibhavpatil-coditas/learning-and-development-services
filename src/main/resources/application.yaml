spring:
  application.name: learning-and-development-services
  datasource:
    url: "jdbc:postgresql://localhost:5432/learning-and-development-services"
    username: "postgres"
    password: "coditas@"
  jpa:
    properties.hibernate.default_schema: "dev"
    hibernate.ddl_auto: create

server:
  servlet:
    context-path: "/api"